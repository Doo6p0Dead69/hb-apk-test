name: healing_bowl_tuner
description: Offline tuner for singing bowls with robust DSP
publish_to: "none"
version: 0.1.0+1
environment: { sdk: ">=3.3.0 <4.0.0" }
dependencies:
  flutter: { sdk: flutter }
  flutter_localizations: { sdk: flutter }
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets: [ assets/test_wavs/ ]
